# TruthMark 🔍  
AI Content Detection Platform

TruthMark is an AI-powered web application that detects whether a given text is likely written by a human or generated by AI. It uses a fine-tuned RoBERTa-based deep learning model with a FastAPI backend and a modern frontend interface.

---

## 🚀 Features
- Detects AI-generated vs Human-written text
- Real-time confidence scoring
- Detailed breakdown of writing patterns
- FastAPI-based backend API
- Modern HTML, CSS, JS frontend
- RoBERTa-based transformer model

---

## 🧠 Model & Tech Stack

**Model**
- RoBERTa-base (Transformer)
- Fine-tuned for AI vs Human classification

**Backend**
- FastAPI
- TensorFlow / tf.keras
- HuggingFace Transformers

**Frontend**
- HTML, CSS, JavaScript

---

## ⚙️ Setup & Run

### Backend
```bash
pip install -r requirements.txt
uvicorn backend.main:app --reload
